# skillfactory Django Project

Для быстрого старта ->

```
python -m venv .venv
.venv/Scripts/activate
pip install -r requirements.txt
cd Newsportal
python manage.py migrate
python manage.py runserver
```

!!! #В разработке !!!
